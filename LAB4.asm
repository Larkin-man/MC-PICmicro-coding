#include<p16f84.inc>

INITB 	EQU B'00000000'
VARA 	EQU 0Ch
VARB 	EQU 0Dh

ORG 0
GOTO BEGIN
ORG 100h

BEGIN
	BCF 	STATUS, RP0
	CLRF 	PORTB
	MOVLW 	INITB
	BSF 	STATUS, RP0
	MOVWF 	TRISB

	BCF 	STATUS, RP0

	MOVLW	D'3'
	MOVWF	VARA

	CLRW
LOOP
	ADDLW	D'4'
	DECFSZ	VARA, 1
	GOTO 	LOOP

	MOVWF	PORTB
END
